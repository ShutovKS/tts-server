-r requirements-base.txt
-r requirements-runtime-qwen.txt
-r requirements-runtime-piper.txt

# Default operator install keeps the stable Qwen + Piper lanes in one environment.
# Install OmniVoice and VoxCPM2 in dedicated runtime environments because their upstream
# dependency stacks may require transformer/library versions that diverge from qwen-tts.
# Optional add-on packs:
#   pip install -r requirements-runtime-omnivoice.txt
#   pip install -r requirements-runtime-voxcpm.txt
#   pip install -r requirements-runtime-qwen-fast.txt
