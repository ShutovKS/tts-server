-r profiles/packs/base/common.txt
-r profiles/packs/module/server.txt
-r profiles/packs/module/telegram.txt
-r profiles/packs/family/qwen.txt
-r profiles/packs/family/piper.txt

# Default operator install keeps the stable Qwen + Piper lanes in one environment.
# The repository now composes family/platform/module packs under profiles/packs/.
# Install OmniVoice in a dedicated runtime environment because its upstream
# dependency stacks may require transformer/library versions that diverge from qwen-tts.
# Optional add-on packs:
#   pip install -r profiles/packs/family/omnivoice.txt
#   pip install -r profiles/packs/family/qwen-fast-addon.txt
